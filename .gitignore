# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by the verification gate: `make coverage` writes coverage/, `tsc -b` writes the
# incremental build caches (which embed absolute paths, so they differ per machine).
coverage
*.tsbuildinfo

# Env
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
